def revers_list() :
    listToRevers = [1, 2, 3, 4, 5]
    listRevers = []
    print("List to revers :listToRevers)
    for i in reversed(listToRevers):
        listRevers.append(i)
    print(listRevers)
revers_list()


    
